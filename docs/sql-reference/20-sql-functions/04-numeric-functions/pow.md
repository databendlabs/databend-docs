---
title: POW
---

Returns the value of `x` to the power of `y`.

## Syntax

```sql
POW( <x, y> )
```

## Examples

```sql
SELECT POW(-2, 2);

┌───────────────┐
│ pow((- 2), 2) │
├───────────────┤
│             4 │
└───────────────┘
```