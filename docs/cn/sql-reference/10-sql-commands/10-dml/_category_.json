```markdown
# DML 命令 {#dml-commands}

DML（数据操纵语言）命令用于在 Databend 中插入、更新、删除和查询数据。

## 插入数据 {#insert}

您可以使用 `INSERT` 语句向 Databend 表中插入数据。

```sql
INSERT INTO table_name (column1, column2, column3, ...)
VALUES (value1, value2, value3, ...);
```

## 更新数据 {#update}

目前，Databend 不支持 `UPDATE` 语句。

## 删除数据 {#delete}

要从 Databend 表中删除数据，您可以使用 `DELETE` 语句。

```sql
DELETE FROM table_name WHERE condition;
```

请注意，`DELETE` 语句的使用可能会对性能产生重大影响。

## 查询数据 {#select}

您可以使用 `SELECT` 语句从 Databend 表中查询数据。

```sql
SELECT column1, column2, ... FROM table_name WHERE condition;
```

<Notes>

- Databend 支持标准的 SQL 查询语法。
- 请确保您的查询语句针对具体的使用场景进行了优化。

</Notes>
```