```markdown
# 文件格式

Databend 支持多种文件格式，用于数据的导入和导出。这些格式包括但不限于 CSV、Parquet、JSON 等。本节将介绍如何在 Databend 中使用这些文件格式。

## CSV {#csv}

CSV（逗号分隔值）是一种常见的数据交换格式，由多行组成，每行代表一个数据记录。在 Databend 中，您可以使用 CSV 格式导入和导出数据。

### 导入 CSV 数据

要导入 CSV 数据，您可以使用 `COPY INTO` 语句。以下是一个示例：

```sql
COPY INTO my_table FROM 's3://my_bucket/my_data.csv' FILE_FORMAT = (TYPE = 'CSV');
```

这将从指定的 S3 存储位置导入 CSV 数据到 `my_table` 表中。

### 导出到 CSV

要将数据导出为 CSV 格式，您可以使用 `COPY INTO LOCATION` 语句。以下是一个示例：

```sql
COPY INTO LOCATION 's3://my_bucket/my_exported_data.csv' FROM my_table FILE_FORMAT = (TYPE = 'CSV');
```

这将把 `my_table` 表中的数据导出到指定的 S3 存储位置。

## Parquet {#parquet}

Parquet 是一种列式存储格式，旨在优化分析型工作负载的性能。Databend 支持使用 Parquet 格式进行数据的导入和导出。

### 导入 Parquet 数据

要导入 Parquet 数据，您可以使用 `COPY INTO` 语句，类似于导入 CSV 数据。以下是一个示例：

```sql
COPY INTO my_table FROM 's3://my_bucket/my_data.parquet' FILE_FORMAT = (TYPE = 'PARQUET');
```

### 导出到 Parquet

要将数据导出为 Parquet 格式，您同样可以使用 `COPY INTO LOCATION` 语句。以下是一个示例：

```sql
COPY INTO LOCATION 's3://my_bucket/my_exported_data.parquet' FROM my_table FILE_FORMAT = (TYPE = 'PARQUET');
```

## JSON {#json}

JSON（JavaScript 对象表示法）是一种轻量级的数据交换格式。Databend 支持使用 JSON 格式进行数据的导入和导出。

### 导入 JSON 数据

要导入 JSON 数据，您可以使用 `COPY INTO` 语句，类似于导入 CSV 或 Parquet 数据。以下是一个示例：

```sql
COPY INTO my_table FROM 's3://my_bucket/my_data.json' FILE_FORMAT = (TYPE = 'JSON');
```

### 导出到 JSON

要将数据导出为 JSON 格式，您可以使用 `COPY INTO LOCATION` 语句。以下是一个示例：

```sql
COPY INTO LOCATION 's3://my_bucket/my_exported_data.json' FROM my_table FILE_FORMAT = (TYPE = 'JSON');
```
```