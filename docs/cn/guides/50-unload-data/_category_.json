```markdown
# 数据卸载 {#data-unloading}

数据卸载是将数据从 Databend 数据仓库导出到其他存储系统的过程。这对于数据备份、数据迁移或将数据移动到不同的分析工具中进行进一步分析等场景非常有用。

## 开始之前 {#before-you-begin}

在开始数据卸载之前，请确保你已经：

- 安装了 Databend CLI
- 配置了对应的存储账户和访问权限

## 使用 SQL 导出数据 {#export-data-using-sql}

你可以使用 `COPY INTO <location>` 语句将查询结果导出到指定的位置。以下是一个示例，展示了如何将查询结果导出到 CSV 文件中：

```sql
COPY INTO 's3://my-bucket/exported-data.csv'
FROM (
    SELECT * FROM my_table
)
FILE_FORMAT = (TYPE = 'CSV');
```

这将会将 `my_table` 的内容导出到名为 `exported-data.csv` 的文件中。

## 使用 Databend CLI 导出数据 {#export-data-using-databend-cli}

除了使用 SQL，你还可以使用 Databend CLI 来导出数据。以下是使用 Databend CLI 导出数据到 CSV 文件的命令示例：

```bash
databend-cli export csv --query "SELECT * FROM my_table" --output s3://my-bucket/exported-data.csv
```

这将执行 SQL 查询并将结果导出到指定的 S3 存储位置。

<Notes>

请注意，导出数据可能会产生额外的存储和传输费用，具体取决于你的存储提供商的定价策略。

</Notes>

## 结论 {#conclusion}

数据卸载是 Databend 数据仓库管理的一个重要方面。通过使用 SQL 语句或 Databend CLI，你可以轻松地将数据导出到不同的存储系统中，以满足你的数据备份、迁移或分析需求。
```
